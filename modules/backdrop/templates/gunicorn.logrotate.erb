/var/log/<%= title %>/*.log* {
  rotate 12
  weekly
  missingok
  compress
  create 0640 <%= user %> <%= group %>
  sharedscript
  postrotate
    kill -USR1 $(initctl status <%= title %> | awk '{ print $4 }')
  endscript
}
